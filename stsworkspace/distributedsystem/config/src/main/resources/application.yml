server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/oncloud7/cloud-config


eureka:
  client:
    service-url:
      defaultZone: http://lijun:1234@localhost:8761/eureka/
  instance:
    prefer-ip-address: true

